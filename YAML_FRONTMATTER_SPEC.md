---
name: YAML_Frontmatter_Specification
description: TML-Docs-Spec 中所有文档模板的 YAML Frontmatter 字段规范说明，包括字段定义、使用场景和遵循规则。
---

# YAML Frontmatter 规范说明

本文档定义了 TML-Docs-Spec 中所有文档模板的 YAML Frontmatter 字段规范，指导在文档模板生成和文档生成场景中的正确使用。

---

## 全局级 Frontmatter（文档最顶部）

全局级 Frontmatter 用于定义整个文档的元数据和整体指导原则。

### 字段定义

| 字段名 | 类型 | 必填 | 描述 | 使用规则 |
|--------|------|------|------|---------|
| `name` | String | ✅ 必填 | 模板的英文/拼音标识符 | 模板创建时：使用英文或拼音，如 `PRD_Spec`、`Meeting_Minutes_Spec`。文档生成时：保持不变，不修改。 |
| `description` | String | ✅ 必填 | 一句话说明这个模板/文档是干什么的 | 模板创建时：描述模板的用途和适用场景。文档生成时：修改为具体的文档描述，如"xxx 项目的架构设计文档"。 |
| `methodology` | Array[String] | 选填（强烈推荐） | 指导写作的理论框架或思维模型 | 模板创建时：列出该模板遵循的方法论，如 `["金字塔原理：结论先行", "SCQA 模型"]`。文档生成时：保持不变，不修改。 |
| `out_of_scope` | Array[String] | ✅ 必填 | 明确指出该文档**绝对不应该**包含的内容 | 模板创建时：列出 1-2 条防越界警告，防止范围蔓延。文档生成时：保持不变，不修改。 |

### 使用规则

**模板创建场景**：
- `name`：使用英文或拼音标识符，确保全局唯一
- `description`：描述模板的通用用途，不涉及具体项目
- `methodology`：列出该模板遵循的理论框架，帮助 Agent 理解写作思路
- `out_of_scope`：明确边界，防止内容蔓延

**文档生成场景**：
- `name`：保持不变（继承自模板）
- `description`：修改为具体的文档描述，如"[项目名称] 架构设计文档"
- `methodology`：保持不变（继承自模板）
- `out_of_scope`：保持不变（继承自模板）

---

## 章节级 Frontmatter（紧跟在 `##` 或 `###` 标题下方）

章节级 Frontmatter 用于定义每个章节的写作要求、格式指导和示例。

### 字段定义

| 字段名 | 类型 | 必填 | 描述 | 使用规则 |
|--------|------|------|------|---------|
| `required` | Boolean | ✅ 必填 | 该章节是否为必填 | 模板创建时：`true` 表示 Agent 必须填写，`false` 表示选填。文档生成时：根据此值判断是否必须生成该章节内容。 |
| `description` | String | ✅ 必填 | 告诉 Agent 这个章节的写作思路和目标 | 模板创建时：详细说明该章节的写作目标、方法论应用、内容要求。文档生成时：作为指导原则，确保生成的内容符合要求。 |
| `methodology` | String | 选填 | 仅该章节适用的特定思维模型 | 模板创建时：如果该章节有特殊的方法论要求，在此指定。文档生成时：保持不变，作为内容组织的指导。 |
| `format_instruction` | String | 选填 | 强制的排版格式要求 | 模板创建时：指定排版要求，如"必须使用 Markdown 表格"、"必须使用 Mermaid 时序图"。文档生成时：严格遵循此要求进行内容排版。 |
| `example` | String (多行块) | 选填（核心章节强烈推荐） | 为 Agent 提供高质量的 Few-shot 示例 | 模板创建时：提供标准的示例内容，帮助 Agent 理解预期的输出格式。文档生成时：**删除此字段及其内容**，仅保留用户的实际内容。 |

### 使用规则

**模板创建场景**：
- `required`：根据章节的重要性判断，必填章节应该是模板的核心内容
- `description`：写给 Agent 看，要具体说明写作思路，如"运用 SCQA 模型，先描述现状（S），再点出冲突（C），最终引出问题（Q）"
- `methodology`：如果该章节有特殊的方法论要求，在此指定
- `format_instruction`：明确排版要求，如"必须使用无序列表"、"必须使用 Markdown 表格"
- `example`：提供高质量的示例，这是提升模板产出质量的最有效手段

**文档生成场景**：
- `required`：根据此值判断是否必须生成该章节。如果 `required: false` 且用户未提供相关信息，可以不生成该章节
- `description`：作为内容组织的指导原则，确保生成的内容符合要求
- `methodology`：保持不变，作为内容组织的指导
- `format_instruction`：严格遵循此要求进行内容排版
- `example`：**必须删除**，不在最终文档中出现

---

## 文档生成时的 Frontmatter 处理

### 全局级 Frontmatter 处理

生成的文档应该**完全移除全局级 Frontmatter**。

**原因**：生成的文档是最终交付物，不需要包含元数据和方法论指导。

**处理方式**：
```
❌ 错误做法：生成的文档包含 YAML Frontmatter
---
name: Project_Pitch_Spec
description: xxx 项目概念介绍文档
methodology: [...]
out_of_scope: [...]
---
# xxx 项目概念介绍

### 章节级 Frontmatter 处理

生成的文档应该**完全移除章节级 Frontmatter**。

**原因**：章节级 Frontmatter 是模板的内部指导，不是文档内容的一部分。

**处理方式**：
```
❌ 错误做法：生成的文档包含章节级 Frontmatter
## 1. 核心目标
---
required: true
description: 描述项目的宏观背景...
---
[内容]

✅ 正确做法：生成的文档不包含任何 Frontmatter
## 1. 核心目标
[内容]
```

---

## 占位符处理规则

### 模板中的占位符

模板中包含以下类型的占位符，需要在文档生成时处理：

| 占位符类型 | 示例 | 处理方式 |
|-----------|------|---------|
| 内容占位符 | `[在此处输出系统上下文...]` | 替换为实际内容或删除（如果 required: false 且无内容） |
| 示例占位符 | `example: \| [高质量示例]` | 删除整个 `example` 字段及其内容 |
| Frontmatter | `---` 包裹的元数据 | 删除所有 Frontmatter（全局级和章节级） |

### 删除规则

文档生成时必须删除以下内容：
1. 所有 YAML Frontmatter（全局级和章节级）
2. 所有 `[在此处输出...]` 类型的占位符说明
3. 所有 `example` 字段及其内容
4. 所有模板中的注释和指导文本

---

## 文件命名规范

### 模板文件命名

模板文件应该使用以下格式：
```
[模板名称]模板.md
```

示例：
- `项目概念介绍模板.md`
- `架构设计模板.md`
- `会议记录模板.md`

### 生成文档文件命名

生成的文档应该使用以下格式：
```
[具体名称][模板类型]文档.md
```

示例：
- `xxx 项目项目概念介绍文档.md`
- `xxx 系统架构设计文档.md`
- `2026-03-29 技术分享会议记录文档.md`

---

## 常见错误与纠正

| 错误 | 原因 | 纠正方式 |
|------|------|---------|
| 生成的文档包含 YAML Frontmatter | 混淆了模板和文档的用途 | 删除所有 Frontmatter，仅保留内容 |
| 生成的文档包含 `example` 字段内容 | 未理解 example 的用途 | 删除 `example` 字段及其内容 |
| 生成的文档包含 `[在此处输出...]` 占位符 | 未完全填充模板 | 替换为实际内容或删除占位符 |
| 文档标题仍是模板名称 | 未修改文档标题 | 修改为具体的项目/内容名称 |
| 创建模板时包含具体项目信息 | 混淆了模板和文档的用途 | 模板应该是通用的，不涉及具体项目 |

---

## 总结

**模板创建**：
- 全局 Frontmatter 包含 `name`、`description`、`methodology`、`out_of_scope`
- 章节级 Frontmatter 包含 `required`、`description`、`methodology`、`format_instruction`、`example`
- 所有字段都有明确的含义和用途

**文档生成**：
- 删除所有 YAML Frontmatter（全局级和章节级）
- 删除所有占位符和 example 内容
- 修改 `description` 为具体的文档描述
- 修改文件名为 `[具体名称][模板类型]文档.md` 格式
- 保留 `name`、`methodology`、`out_of_scope` 的含义（虽然不在最终文档中出现，但指导内容生成）
